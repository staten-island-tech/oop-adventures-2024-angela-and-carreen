import random

class archer:
    def __init__ (self, name, hp, attack, defense, arrows)
        self.name = name
        self.hp = hp
        self.attack = attack
        self.defense = defense
        self.arrows = arrows

    def attack(self, atttack):
        if self.arrow > 0:
            self

    def take_damage(self, hp):
        self.hp -= damage
        if self.hp < 0:
            print("You are die")
        else:
            print(f"You have {self.hp} HP left")
    


Archer = archer("user", 100, 10, 10, 15)